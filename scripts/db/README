This command will create the database, run schema, and add some initial data (including the root user). You must configure config.ini, config-user.ini, and config-sms.ini prior to calling this. The database must be running prior to calling this.
The args are:
 * devMode (not required, but will set up the 'test' database if an argument is provided)
 
./setupDBServer.sh 1


This command will clean the db server. You must configure config.ini prior to calling this. The database must be running prior to calling this.

./clean.sh